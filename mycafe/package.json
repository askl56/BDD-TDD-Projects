{
  "name": "mycafe",
  "version": "1.0.0",
  "description": "cafe",
  "main": "index.js",
  "scripts": {
    "test": "mocha -u bdd -R spec -t 500 --recursive",
    "watch": "mocha -u bdd -R spec -t 500 --recursive --watch"
  },
  "author": "amscott@openmailbox.org",
  "license": "ISC",
  "devDependencies": {
    "chai": "^2.2.0",
    "mocha": "^2.2.4",
    "sinon": "^1.14.1"
  }
}
